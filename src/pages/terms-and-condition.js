import Layout from '@/components/layout'
import React from 'react'

export default function termsAndCondition({ header, footer }) {
  return (
    <Layout header={header} footer={footer}>
      <h1 className='container mx-auto'> Terms & Condition</h1>
    </Layout>
  )
}

